def fn(arr):
    """Two Pointers: one input, opposite ends"""
    left = ans = 0
    right = len(arr) - 1
    
    while left < right:
        # do some logic here with the left and right
        if CONDITION:
            left += 1
        else:
            right -= 1
            
    return ans